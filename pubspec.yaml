name: background_downloader_sql
description: "Adds an SQLite database to background_downloader, and migration options for other databases including flutter_downloader"
version: 1.0.1
repository: https://github.com/781flyingdutchman/background_downloader_sql
publish_to: "none"

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  background_downloader:
    git:
      url: https://github.com/nodejs-Devinium/background_downloader.git
      ref: 96f2731f1c8a388b41c235be808a1309af559310 # commit
  logging: ^1.0.2
  path: ^1.8.1
  path_provider: ^2.0.2
  sqflite: ^2.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1


